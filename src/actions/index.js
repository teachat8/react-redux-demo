export const checked = (id) => ({
  type: 'ADD',
  id
})

export const move = (id) => ({
  type: 'MOVE',
  id
})

export const come = (id) => ({
  type: 'COME',
  id
})

export const res = (id) => ({
  type: 'RES',
  id
})

export const add = (id) => ({
  type: 'ADD_NUM',
  id
})

export const delete1 = (id) => ({
  type: 'DELETE',
  id
})

export const toogle_model = () => ({
  type: 'TOOGLE_MODEL'
})
